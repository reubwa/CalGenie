<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CalGenie.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalGenie"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CalGenie">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TitleBar
            Title="CalGenie"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="False"
            x:Name="tb">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/calgenie-icon.ico" />
            </TitleBar.IconSource>
        </TitleBar>
        <NavigationView x:Name="nvSample"
                        IsBackButtonVisible="Collapsed"
                        IsPaneToggleButtonVisible="False"
                        Grid.Row="1"
                        PaneDisplayMode="Top"
                        Loaded="NavView_Loaded"
                        ItemInvoked="NvSample_OnItemInvoked">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Import Calendar" Tag="importCal" x:Name="PgImportCal">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B5;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Add Tasks" Tag="addTasks" x:Name="AddTask">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7C4;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="See Results" Tag="seeResults" x:Name="ResultsPg">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8BC;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <Frame x:Name="contentFrame" Navigated="ContentFrame_OnNavigated"/>
        </NavigationView>
    </Grid>
</Window>
