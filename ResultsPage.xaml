<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CalGenie.ResultsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalGenie"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Margin="10" Padding="10" Loaded="FrameworkElement_OnLoaded">
        <ListView x:Name="LstEvents">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:CalendarEvent">
                    <Grid Padding="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{x:Bind Summary}"/>
                        <TextBlock Text="{x:Bind StartTime}" Grid.Row="1"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
